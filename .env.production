# Production env for Mi Gente Latino.
# These are Supabase PUBLIC keys — designed to be exposed in the browser.
# Data is protected by Row Level Security (anon can only INSERT, never read
# the signups list), so committing them here is safe and standard.
NEXT_PUBLIC_SUPABASE_URL=https://xdjwoumeabppdsmjplys.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable__0ODq4OtHnd_IGT--APigg_UPKb1Slk
